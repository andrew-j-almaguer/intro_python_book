# Without running the following code, what do you think it will do?
def foo(bar, qux):
    print(bar)
    print(qux)

foo('Hello')

# The code will raise an error because it has been passed too few arguments.